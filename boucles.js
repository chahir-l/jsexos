console.log('test')

let fisrtAr = ['hello', 'every', 'one']
/**
 * display each element of the array
 */
for (let i=0; i< fisrtAr.length; i++)
{
    console.log(fisrtAr[i] + "\n")
}

/**
 * While (until 10)
 */

let cpt = 1;

while(cpt <= 10)
{
    console.log(cpt)
    cpt++;
}
